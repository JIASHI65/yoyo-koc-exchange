# Yoyo KOC System E2E 测试计划

**版本**：2026-08-27
**目标**：验证创作者注册到奖励发货的真实闭环。
**执行原则**：只使用专用测试账号；不触发 MochiBot；不修改现有正式 KOC 数据。

## 1. 测试数据

统一测试标识：`E2E-20260827-A`
Discord：`E2E-20260827-A`
Game UID：`E2ETESTAUG27`
Account ID：`6123456789012345678`（19 位）
姓名：`E2E Test Creator`
Server：`Beacon`
地址：`123 Test Street, Test City, CA 90001, USA`
电话：`+12025550127`

作品链接使用不对应真实创作者的公开格式测试链接，并统一包含 `e2e-20260827` 标识。

## 2. 测试前保护

1. 记录当前 Git 提交。
2. 记录当前结算期。
3. 记录投稿开关、兑换开关。
4. 记录投稿截止时间、兑换截止时间。
5. 记录测试 UID 在所有相关表中的初始行数。
6. 确认 MochiBot 自动欢迎关闭，不进入广播页，不点击任何发送按钮。
7. 只删除测试 UID 产生的数据，不使用模糊条件删除生产数据。

相关表：
- `registration_applications`
- `kocs`
- `submissions`
- `point_logs`
- `redemption_orders`
- `score_imports`
- `audit_logs`（若存在）

## 3. 测试阶段

### Phase 1：注册与审核

1. 使用 18 位和 20 位 Account ID 提交，预期前端阻止。
2. 使用正确 19 位 Account ID 提交，预期生成一条 pending 申请。
3. 批准前尝试登录，预期失败并提示等待审核或无匹配账号。
4. 使用相同 Account ID、Game UID 或 Discord 再次申请，预期不产生重复 pending 申请。
5. 管理员批准申请，预期：
   - `kocs` 生成一条记录；
   - 申请变为 approved；
   - 自动生成一条 0 分欢迎周边 pending 订单；
   - 新人月写为审核后的下一个完整自然月。
6. 批准后登录，预期成功。
7. 尝试重复批准，预期不重复创建 KOC 或欢迎周边。
8. 验证整个阶段没有发送 Discord DM。

### Phase 2：当前月份与活动窗口

1. 验证管理端当前结算期与 `is_current_period=true` 一致。
2. 验证创作者端默认显示同一结算期。
3. 验证 My Submissions 不提供未来月份。
4. 人工关闭投稿，前端提交必须失败。
5. 人工开启投稿，前端提交恢复。
6. 设置已过期投稿截止时间，即使开关开启也必须禁止投稿。
7. 清除/调整截止时间后投稿恢复。
8. 对兑换开关和兑换截止时间重复上述测试。
9. 恢复测试前的月份和窗口配置。

### Phase 3：投稿次数、拒绝与 Showcase

1. 第一次常规投稿放入 10 条不同链接，预期成功。
2. My Submissions 显示 pending、正确月份和 10 条有效链接。
3. 刷新页面和重新登录，记录仍存在。
4. 后台作品审核能够找到相同链接和测试 UID。
5. 第二次常规投稿放入 5 条不同链接，预期成功。
6. 第三次常规投稿预期被阻止。
7. 提交 Showcase，预期成功且不占两次常规投稿机会。
8. 管理员拒绝第一条常规投稿，预期释放一次投稿机会。
9. 再提交一条常规投稿，预期成功。
10. Rejected 卡片明确显示不计入次数和 CC。

### Phase 4：CC 持续创作奖励

使用最多两次常规投稿组合链接数量，不通过十几次投稿凑数。

1. 去重有效链接为 14，CC 不达标。
2. 增加到 15，CC 达标。
3. 增加重复链接，CC 仍按去重后的数量计算。
4. 拒绝包含部分链接的投稿，CC 立即排除该投稿链接。
5. 恢复有效链接 ≥15 后，CC 达标。
6. 确认 CC 订单为 pending，且不会重复生成。
7. 手动确认后，订单出现在端内奖励模块。

### Phase 5：算分导入与 My Submissions

1. 为同一测试 UID 准备两条已评分常规投稿：基础分 6 和 12。
2. 导入前检查预览：UID、月份、链接和积分均正确。
3. 确认导入后：
   - 两条投稿状态为 scored；
   - 作品基础分合计 18；
   - point_logs 写入本期应到账积分；
   - 后台能够显示结果。
4. 另外加入新人 +2、Showcase +1、人工 +3。
5. My Submissions 应显示：
   - Base Content Score = 18；
   - Bonus Points = 6；
   - Total Added This Month = 24；
   - Valid Work Links 为去重后的有效作品数量。
6. 重复导入相同月份，不能重复到账。
7. Pending、Scored、Rejected 记录状态和说明正确。

### Phase 6：等级升级

创建隔离的等级历史测试数据，不修改正式创作者等级。

1. 单月基础分 ≥5，不升级 Gold。
2. 连续两个月基础分 ≥5，升级 Gold。
3. 连续三个月基础分 ≥5，升级 Platinum。
4. 基础分 4、奖励分使总到账超过 5，不得计为合格月份。
5. 已是 Gold 或 Platinum 时，后续低分不能自动降级。
6. 同月重复执行自动升级，等级历史只能保留一条该月份记录。

### Phase 7：积分、兑换与发货

1. 使用 point_logs 建立 20 分测试余额。
2. 提交 2 分 Google Play 兑换：
   - 创建 pending 订单；
   - 流水余额仍为 20；
   - 可用积分显示 18。
3. 快速重复点击确认，不能重复建单。
4. 再提交 5 分周边兑换，可用积分变为 13。
5. 尝试兑换超过可用积分的奖励，必须被阻止。
6. 取消其中一条 pending 订单，预留积分立即释放。
7. 管理员发货有效订单：
   - 订单变为 shipped；
   - point_logs 写入一条对应负流水；
   - 余额正确减少。
8. 重复点击发货，不能重复扣分。
9. 发货 0 分欢迎周边，不能产生负流水。
10. My Orders 状态、数量、积分与后台一致。

### Phase 8：持久化与跨设备

1. 创作者端提交后刷新，数据仍存在。
2. 退出后重新登录，投稿、余额和订单仍存在。
3. 管理端刷新后仍能收到投稿和订单。
4. 桌面浏览器与手机浏览器登录同一测试账号，数据一致。
5. 两个设备先后操作后再次刷新，不出现旧数据覆盖新数据。

### Phase 9：响应式与英文界面

测试尺寸：
- 1440×900 桌面端
- 768×1024 平板竖屏
- 390×844 手机竖屏
- 360×800 小屏手机
- 844×390 手机横屏

逐尺寸检查：
- 登录和注册字段可见、可输入。
- 导航按钮可点击且不重叠。
- Submit Works 可提交多条链接。
- My Submissions 四张摘要卡和记录卡不溢出。
- 月份下拉可用且默认当前结算期。
- Redeem Rewards 和 My Orders 不裁切。
- 页面无异常横向滚动。
- 创作者端业务文案均为英文。

### Phase 10：清理与恢复

1. 按测试 UID 精确删除测试订单。
2. 按测试 UID 精确删除测试流水。
3. 按测试 UID 精确删除测试投稿。
4. 删除测试 score_imports 记录。
5. 删除测试 KOC。
6. 删除测试注册申请。
7. 删除测试 audit_logs（如有且可安全识别）。
8. 恢复测试前结算期、开关和截止时间。
9. 再次查询所有相关表，确认测试 UID 行数为 0。
10. 确认正式 KOC 总数和关键配置没有意外变化。

## 4. 缺陷等级

- **P0**：数据丢失、后台收不到投稿、串月份、重复积分/扣分、无法登录/投稿/兑换、误发 Discord 消息。
- **P1**：核心数字错误、等级错误、CC 错误、手机端核心流程无法完成。
- **P2**：文案、排版或非阻塞体验问题。

## 5. 交付物

测试结束必须提供：
- 更新后的 `TESTING_REPORT.md`。
- 填写完成的 `E2E_MANUAL_CHECKLIST.md`。
- 每个失败项的步骤、预期、实际结果和截图/日志。
- 测试数据清理确认。
- 明确的“可上线 / 暂不可上线”结论。
