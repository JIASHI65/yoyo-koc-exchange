# Yoyo KOC 兑换系统 · 产品需求文档（PRD）

> 版本：v1.0 ｜ 更新：2026.07 ｜ 负责人：Arissa ｜ 范围：欧美 KOC 社区

## 一、一句话说清楚这是什么

这是一个给海外 KOC 用的**积分兑换系统**：创作者交作品 → 拿积分 → 换奖励；运营在后台审核、算分、发奖、对账。

一句话概括分工：

- **创作者端**：交作品、看积分、换东西。
- **管理端**：审核、给分、发奖、查账。
- **数据层**：Supabase，积分以流水为准，不信任何缓存余额。

## 二、为什么要做（背景）

在这个系统之前，KOC 运营基本靠**手动**：私信收链接、Excel 记积分、人肉发奖、月底对账靠翻聊天记录。人一多就三个问题：

1. **算不清**：谁交了什么、拿了多少分、换过没有，全靠脑子和表格。
2. **容易错**：重复发奖、漏扣分、跨月算错，出一次就是真金白银的损失。
3. **不可追溯**：出了问题查不到是谁、什么时候、改了什么。

所以目标很直接：**把这套流程产品化，让一个人也能稳定管住几十个创作者的积分和奖励，且每一步可查、可对账、不出钱的错。**

不追求大而全，追求**够用、稳、能查账**。

## 三、给谁用（用户）

| 角色 | 是谁 | 核心诉求 |
|---|---|---|
| 创作者 / KOC | 欧美签约创作者 | 交作品简单、积分清楚、兑换顺畅 |
| 运营（你） | 单人管理 | 审核快、发奖不出错、月底能对上账 |
| 发货系统 | 老板那套独立系统 | 接收欧美订单去发货（不在本系统内） |

## 四、核心原则（做之前先定死）

1. **积分只认流水**：余额 = `point_logs` 求和，任何地方都不直接改「余额字段」当真相。
2. **钱相关的操作必须防重**：重复发货不重复扣分，批量发奖同人同期同奖励自动跳过。
3. **危险操作先确认**：删除、扣分、发货、批量操作都要二次确认。
4. **一切可追溯**：关键动作写 `audit_logs`，出问题能查到人和时间。
5. **不过度设计**：500 人以内、单人运营、0 预算，不上复杂 Auth / RLS / 微服务。

## 五、功能模块（创作者端）

### 5.1 注册 / 登录
- 登录方式：**UID + Account ID 双重校验**（防止只凭 UID 冒登他人）。
- 校验失败：拒绝进入，提示核对信息。
- 新用户：填写 Discord 名、Account ID、收货信息 → 生成 UID → 入库 `kocs`。

### 5.2 投稿
- 类型一：**作品(work)**，粘贴平台链接，按平台数据计分。
- 类型二：**showcase**，每人每期只 +1 分（防刷）。
- 提交前做链接有效性校验，空 / 非法链接拦截。
- 提交后状态为「待审核」，写入 `submissions`。

### 5.3 兑换
- 奖励项：`450 钻石(1分)` / `Google Play $10(2分)` / `周边礼包(5分)`。
- 兑换前校验：**积分足够 + 兑换开放中**。
- 通过后生成 `redemption_orders`，积分按流水扣减。
- 积分不足 / 兑换关闭：拦截，不生成订单。

### 5.4 历史 & FAQ
- 查看个人投稿记录、积分明细、兑换订单，按月份展示。
- 内置中英文 FAQ，覆盖规则、结算、兑换、联系方式。

## 六、功能模块（管理端）

### 6.1 KOC 信息库
- 新增 / 编辑 / 批量标记（新老创作者）/ 批量删除。
- 支持地址、Account ID、Server、备注等资料维护。

### 6.2 投稿审核 & 评分
- 按**当前 period** 过滤，避免跨月混算。
- 支持单条拒绝、批量标记已评分。
- showcase 通过 +1 分 / 拒绝。
- 评分写入 `point_logs`，同步到创作者端。

### 6.3 积分操作
- 手动加分 / 扣分（不允许扣成负数）。
- 支持直接改余额（inline），来源统一记为 `manual`。

### 6.4 奖励发放 / 发货
- 标记发货 = **系统内账务状态**（标记 + 按需扣分），物理物流在独立发货系统。
- **重复发货不重复扣分**。
- 批量发奖：同人 + 同期 + 同类同名奖励**自动去重**。

### 6.5 活动配置
- 计分规则、月度上限、等级门槛配置。
- 投稿开关、兑换开关、投稿截止时间。

### 6.6 月结对账（只读）
- 一键扫描本期异常：重复订单 / 重复 showcase / 疑似未扣分 / period 缺失 / 异常流水来源。
- **只列异常，不自动改数据**，人工确认后处理。

### 6.7 操作日志
- 关键动作写 `audit_logs`：配置变更、审核、评分、发货、发奖、KOC 增改、手动积分调整等。

## 七、计分规则（当前活动 · 2026.07）

| 平台 | 计分口径 |
|---|---|
| TikTok | 按播放量：200 views = 1 分，400 views = 2 分 |
| YouTube | 按播放量：200 views = 1 分（含观看时长），400 views = 2 分 |
| IG / FB / Reddit / X / Pinterest | 按互动量：40 互动 = 1 分，80 互动 = 2 分 |
| showcase | 每人每期 +1 分 |

- **月度上限 40 分**，超出不再累积，下月重置。
- 持续创作奖励：当月有效投稿链接 ≥15 条，低互动也计入；被拒绝作品不计入。
- 具体数值以「活动配置」页为准，规则可后台调整。

## 八、数据模型（核心表）

| 表 | 作用 | 关键点 |
|---|---|---|
| `kocs` | 创作者主数据 | UID 唯一，`__config__` 行存系统配置 |
| `submissions` | 投稿记录 | work / showcase，带 period |
| `point_logs` | 积分流水（真相源） | source: manual / redemption / auto_settlement |
| `redemption_orders` | 兑换订单 | 带 period、status、points_spent |
| `campaign_config` | 活动配置 | 计分规则、开关、上限 |
| `audit_logs` | 操作留痕 | action_type + operator + detail |

**余额定义**：`SUM(point_logs.change) where uid = ?`，不依赖任何缓存字段。

## 九、数据守卫（已在 Supabase 手动执行）

- `period` 缺失回填 + 格式校验（`YYYY-MM`）。
- `account_id` 非空唯一（排除 `__config__`）。
- showcase 每人每期唯一。
- 兑换订单同人同期同奖励去重（非 cancelled）。
- `audit_logs` 加查询索引。

## 十、不做什么（明确边界）

- ❌ 不做物理发货 / 物流（在老板的独立发货系统）。
- ❌ 不做复杂账号体系（后台仅简单密码，内部使用）。
- ❌ 不做 Supabase RLS 全量落地 / 自动回滚系统。
- ❌ 不做前端框架重构（当前为原生 HTML/JS，模块化列为后续技术债）。

## 十一、成功标准

1. 单人可稳定管理数十个 KOC 的投稿、积分、兑换。
2. 不出现重复发奖 / 漏扣分 / 跨月算错的资损问题。
3. 任一关键操作可在审计日志追溯到人和时间。
4. 月底对账能一键发现异常，无需翻聊天记录。

## 十二、后续迭代（技术债 & 优化）

| 优先级 | 事项 | 说明 |
|---|---|---|
| 高 | 一键导出欧美待发货订单 | 方便交接给发货系统 |
| 中 | 审计日志记录改前/改后值 | 更强追溯 |
| 中 | 第一阶段模块化 | 抽出 config/api/period/points 等公共模块 |
| 低 | FAQ / 文案持续同步 | 与最新规则对齐 |

---

_文档随系统迭代更新。流程图见 `flowcharts.html`（浏览器打开可直接截图）。_


