# Yoyo KOC System 上线前测试报告

**文档版本**：2026-08-27  
**适用代码版本**：`a05d07e` 及之后  
**系统地址**：
- 创作者端：https://yoyo-creative-studio.github.io/yoyo-koc-exchange/index.html
- 管理员端：https://yoyo-creative-studio.github.io/yoyo-koc-exchange/admin.html

## 1. 当前结论

**状态：待执行完整 E2E 测试，暂不能仅凭静态检查宣布上线通过。**

目前已经确认：
- 创作者端和管理员端 JavaScript 可以正常解析。
- GitHub Pages 已部署当前版本。
- 主要业务模块均存在。
- 本报告使用当前业务口径，不再沿用旧测试文档中的错误预期。

尚未确认：
- 真实注册、审核、投稿、算分、兑换、发货闭环是否全部通过。
- 手机、平板和桌面端是否均无阻塞性 UI 问题。
- 刷新、重新登录和跨设备后的数据是否始终一致。
- 测试数据是否能够安全、完整地清理。

## 2. 严格禁止范围

本轮测试不得执行以下操作：
- MochiBot 广播。
- MochiBot 条件私信。
- MochiBot CSV 个性化发送。
- Discord 新人自动欢迎。
- 任何真实 Discord DM。
- 修改、清洗或删除现有正式 KOC 数据。

测试注册审核只验证系统数据流程。任何可能发送 Discord 消息的入口都必须跳过。

## 3. 当前正确业务口径

### 3.1 注册与审核
- Account ID / System ID 必须严格为 19 位数字。
- 新申请先进入 `registration_applications`，状态为 `pending`。
- 管理员批准后才写入 `kocs`，创作者才能登录。
- 新注册并批准的创作者自动生成一份 0 分欢迎周边订单。
- 批量导入旧 KOC 不生成欢迎周边。
- 新人月是“审核通过后的下一个完整自然月”。
- 重复申请、重复批准不能产生重复 KOC 或重复欢迎周边。

### 3.2 月份与活动窗口
- `campaign_config.is_current_period=true` 的月份是系统唯一当前结算期。
- 管理端保存的当前期应同步到创作者端。
- 创作者端不能选择未来月份提交或查看未来记录。
- 投稿和兑换均同时受人工开关与自动截止时间控制。
- 人工关闭或超过截止时间，创作者端都必须禁止操作。

### 3.3 投稿
- 每个结算期最多 2 次有效常规投稿。
- 一次投稿可以包含多条作品链接。
- 第三次有效常规投稿必须被阻止。
- 被拒绝的常规投稿释放一次投稿机会。
- Showcase 不占 2 次常规投稿机会。
- 刷新或重新登录后，投稿记录不能消失。

### 3.4 CC 持续创作奖励
- 仅统计当前期、非 Showcase、未被拒绝的有效作品链接。
- 链接去重后达到 15 条即符合 CC。
- 不要求连续发布 5 天。
- 被拒绝链接和重复链接不能计入 CC。
- CC 达标后生成待管理员确认的端内奖励，不能重复生成。

### 3.5 作品基础分与等级
- 作品基础分来自 `submissions.points_earned`。
- 只统计当月 `status=scored` 且非 Showcase 的投稿。
- 同月多条已评分投稿需要相加。
- 新人 `+2`、Showcase `+1`、人工加分、历史余额和兑换不计入作品基础分。
- Gold：连续 2 个月作品基础分 ≥5。
- Platinum：连续 3 个月作品基础分 ≥5。
- 等级只能升级，不能自动降级。
- 同月重复执行等级升级不能重复或破坏等级历史。

### 3.6 积分与兑换
- 余额真相源是 `point_logs` 的 `SUM(change)`。
- 提交兑换后创建 `pending` 订单，积分只被预留，不立即写负流水。
- 可用积分 = 流水余额 - pending 订单预留积分。
- 管理员标记发货时才写入兑换负流水。
- 重复点击提交兑换不能重复建单。
- 重复点击发货不能重复扣分。
- 欢迎周边是 0 分订单，发货不能扣积分。
- 取消 pending 订单后，预留积分应释放。

## 4. 必测范围

| 阶段 | 测试主题 | 上线阻塞 |
|---|---|---|
| 1 | 测试前快照与隔离 | 是 |
| 2 | 注册、重复申请、审核、欢迎周边 | 是 |
| 3 | 当前月份、人工开关、自动截止 | 是 |
| 4 | 两次投稿、第三次阻止、拒绝释放、Showcase | 是 |
| 5 | CC 去重、拒绝排除、15 条边界 | 是 |
| 6 | 算分导入、基础分、奖励分、重复导入 | 是 |
| 7 | Gold / Platinum 只升不降 | 是 |
| 8 | 兑换预留、发货扣分、重复操作 | 是 |
| 9 | 后台收件、刷新、重新登录、跨设备 | 是 |
| 10 | 桌面、平板、手机 UI | 是 |
| 11 | 测试数据清理与配置恢复 | 是 |

## 5. 上线通过标准

必须同时满足：
- 所有 P0 核心流程通过。
- 不存在数据消失、串月份、重复积分、重复扣分或重复订单。
- 创作者提交后，后台能够稳定收到数据。
- 手机端能够完成登录、投稿、查看投稿、兑换和查看订单。
- 创作者端不出现中文业务文案。
- 测试账号、投稿、订单、流水和导入记录全部清理。
- 当前结算期、投稿开关、兑换开关和截止时间恢复到测试前状态。
- 测试全程没有触发 MochiBot 或 Discord 消息。

以下任一情况出现则不能上线：
- 投稿成功后刷新消失。
- 后台收不到创作者投稿。
- 投稿或兑换写入错误月份。
- 奖励分被计入等级基础分。
- 重复导入或重复点击导致重复积分/扣分。
- pending 订单与余额预留不一致。
- 测试数据无法确认完整清理。

## 6. 测试结果记录

| 阶段 | 状态 | 失败数量 | 证据/备注 |
|---|---|---:|---|
| 测试前快照 | 未执行 | - | - |
| 注册审核 | 未执行 | - | - |
| 月份与窗口 | 未执行 | - | - |
| 投稿与拒绝 | 未执行 | - | - |
| CC 奖励 | 未执行 | - | - |
| 算分与基础分 | 未执行 | - | - |
| 等级升级 | 未执行 | - | - |
| 兑换与发货 | 未执行 | - | - |
| 持久化与跨设备 | 未执行 | - | - |
| 响应式 UI | 未执行 | - | - |
| 数据清理 | 未执行 | - | - |

## 7. 最终签字

- [ ] 完整 E2E 已执行。
- [ ] 所有 P0 项通过。
- [ ] 失败项已修复并复测。
- [ ] 测试数据已完整清理。
- [ ] 生产配置已恢复。
- [ ] MochiBot / Discord 消息功能未被触发。
- [ ] 系统可以上线。

**最终结论：未签字，等待执行测试。**
